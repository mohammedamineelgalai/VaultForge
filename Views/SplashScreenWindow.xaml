<Window x:Class="XnrgyEngineeringAutomationTools.Views.SplashScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource WindowTitleSplashScreen}"
        Height="400" Width="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True">
    
    <Border Background="#1a1a2e" CornerRadius="16" BorderBrush="#00d4ff" BorderThickness="2">
        <Border.Effect>
            <DropShadowEffect Color="#00d4ff" BlurRadius="20" ShadowDepth="0" Opacity="0.4"/>
        </Border.Effect>
        
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo et Titre -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                <Image Source="/Resources/XnrgyEngineeringAutomationTools.png" 
                       Width="64" Height="64" 
                       Margin="0,0,15,0"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="XNRGY Engineering" 
                               FontSize="24" FontWeight="Bold" 
                               Foreground="White"/>
                    <TextBlock Text="Automation Tools" 
                               FontSize="20" FontWeight="SemiBold" 
                               Foreground="#00d4ff"/>
                </StackPanel>
            </StackPanel>

            <!-- Version - Valeur chargee depuis AppInfo -->
            <TextBlock x:Name="VersionText" Grid.Row="1" 
                       Text="v1.0.0" 
                       FontSize="12" 
                       Foreground="#888888"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <!-- Separateur -->
            <Border Grid.Row="2" Height="1" Background="#00d4ff" Opacity="0.3" Margin="0,0,0,25"/>

            <!-- Zone de statut avec animation -->
            <StackPanel Grid.Row="3" VerticalAlignment="Center">
                <!-- Spinner animé -->
                <Grid HorizontalAlignment="Center" Margin="0,0,0,20">
                    <Ellipse Width="50" Height="50" 
                             Stroke="#252540" StrokeThickness="4"/>
                    <Ellipse x:Name="SpinnerRing" Width="50" Height="50" 
                             Stroke="#00d4ff" StrokeThickness="4"
                             StrokeDashArray="2 4"
                             RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotate" Angle="0"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation 
                                            Storyboard.TargetName="SpinnerRotate"
                                            Storyboard.TargetProperty="Angle"
                                            From="0" To="360" 
                                            Duration="0:0:1.5"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                </Grid>
                
                <!-- Message de statut -->
                <TextBlock x:Name="StatusText" 
                           Text="Initialisation..."
                           FontSize="14"
                           Foreground="#e0e0e0"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"/>
            </StackPanel>

            <!-- Barre de progression -->
            <Grid Grid.Row="4" Margin="0,20,0,0">
                <Border Background="#252540" CornerRadius="4" Height="8"/>
                <Border x:Name="ProgressBar" 
                        Background="#00d4ff" 
                        CornerRadius="4" 
                        Height="8"
                        HorizontalAlignment="Left"
                        Width="0">
                    <Border.Effect>
                        <DropShadowEffect Color="#00d4ff" BlurRadius="8" ShadowDepth="0" Opacity="0.6"/>
                    </Border.Effect>
                </Border>
            </Grid>

            <!-- Pourcentage -->
            <TextBlock x:Name="ProgressText" 
                       Grid.Row="5"
                       Text="0%"
                       FontSize="12"
                       Foreground="#888888"
                       HorizontalAlignment="Center"
                       Margin="0,8,0,0"/>

            <!-- Copyright -->
            <TextBlock Grid.Row="6"
                       Text="By Mohammed Amine Elgalai - XNRGY Climate Systems ULC"
                       FontSize="10"
                       Foreground="#555555"
                       HorizontalAlignment="Center"
                       Margin="0,15,0,0"/>
        </Grid>
    </Border>
</Window>
